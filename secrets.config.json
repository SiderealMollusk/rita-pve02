{
  "secrets": [
    {
      "name": "TF_VAR_PROXMOX_API_TOKEN",
      "opPath": "op://VAULT/PROXMOX_API_TOKEN/value",
      "strategy": "proxmox-api-token",
      "description": "Proxmox API token for Terraform provisioning"
    },
    {
      "name": "TF_VAR_SSH_PUBLIC_KEY",
      "opPath": "op://VAULT/VM_INIT_SSH_PUBLIC_KEY/value",
      "strategy": "ssh-public-key",
      "description": "SSH public key for VM initialization"
    },
    {
      "name": "VM_INIT_SSH_PRIVATE_KEY",
      "opPath": "op://VAULT/VM_INIT_SSH_PRIVATE_KEY/value",
      "strategy": "ssh-private-key",
      "description": "SSH private key for VM initialization (Tailscale SSH used at runtime)"
    },
    {
      "name": "TAILSCALE_AUTH_KEY",
      "opPath": "op://VAULT/TAILSCALE_AUTH_KEY/value",
      "strategy": "tailscale-auth-key",
      "description": "Tailscale auth key for production VM connections (reusable, tag:vm)"
    },
    {
      "name": "TAILSCALE_API_TOKEN",
      "opPath": "op://VAULT/TAILSCALE_API_TOKEN/value",
      "strategy": "tailscale-api-token",
      "description": "Tailscale API token for pve02-dev-container-sessions (generates ephemeral keys)"
    },
    {
      "name": "ARGOCD_ADMIN_PASSWORD",
      "opPath": "op://VAULT/ARGOCD_ADMIN_PASSWORD/value",
      "strategy": "argocd-admin-password",
      "description": "ArgoCD initial admin password (fetched from cluster)"
    }
  ],
  "sshTargets": [
    {
      "name": "proxmox",
      "hostOpPath": "op://VAULT/PROXMOX_SSH_HOST/value",
      "user": "root",
      "port": 22,
      "keyName": "VM_INIT_SSH_PRIVATE_KEY"
    }
  ]
}
